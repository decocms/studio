[
  "daemon e2e: SSE wire shapes — file-changed > a file written outside the fs routes still emits file-changed",
  "daemon e2e: SSE wire shapes — lifecycle, branch, scripts > cloning a repo with no start script drives all three to a terminal, coherent state",
  "daemon e2e: git (cloned repo) > POST /edit refuses an edit that would invalidate a block",
  "daemon e2e: git (cloned repo) > POST /git/publish pushes past a failing pre-push hook",
  "daemon e2e: git (cloned repo) > POST /git/publish refuses to commit an invalid block",
  "daemon e2e: git (cloned repo) > POST /write refuses an invalid decofile block",
  "daemon e2e: git (cloned repo) > SIGTERM skips an invalid block but still syncs the rest",
  "daemon e2e: git (cloned repo) > SIGTERM triggers a graceful publish to origin before exit",
  "daemon e2e: git (diverged origin) > interactive publish reconciles a diverged origin/<branch>",
  "daemon e2e: git (diverged origin) > re-bootstrap fast-forwards an idle branch to a moved base",
  "daemon e2e: git (protected branch) > POST /git/publish on main → 409 and creates no commit",
  "daemon e2e: orgfs-config > invalid org-fs config returns 400",
  "daemon e2e: orgfs-config > returns { written: false } when no sidecar path is configured",
  "daemon e2e: orgfs-config > writes the config to the sidecar control path when configured",
  "daemon e2e: smoke, CORS, dual-prefix > GET /_sandbox/idle and legacy /_decopilot_vm/idle both work without auth",
  "daemon e2e: smoke, CORS, dual-prefix > OPTIONS preflight returns 204 + CORS headers (no auth)",
  "daemon e2e: smoke, CORS, dual-prefix > POST /_decopilot_vm/bash (legacy prefix) with bearer is not 401/404",
  "daemon e2e: smoke, CORS, dual-prefix > no-auth GETs tolerate an arbitrary Authorization header",
  "daemon e2e: smoke, CORS, dual-prefix > serves no-auth GETs and sets CORS on the native branch",
  "daemon e2e: smoke, CORS, dual-prefix > sets Access-Control-Allow-Origin=* on every response branch",
  "with a live loopback origin allowed &gt; daemon e2e: url transfer SSRF gate > write_from_url does not follow a redirect off the allowlist",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects a non-http scheme",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects a suffix of a listed host",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects an unlisted host",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects cloud metadata",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects ip-literal loopback",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects ipv6 loopback",
  "with an allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url rejects plain http on a listed host",
  "with no allowlist configured &gt; daemon e2e: url transfer SSRF gate > upload_to_url denies every host (fail closed)",
  "with no allowlist configured &gt; daemon e2e: url transfer SSRF gate > write_from_url denies every host (fail closed)"
]
