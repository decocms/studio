[
  "daemon e2e: swappable spawn target > defaults to the built Go daemon binary when DAEMON_E2E_CMD is unset",
  "daemon e2e: smoke, CORS, dual-prefix > POST /_decopilot_vm/bash (legacy prefix) with bearer is not 401/404",
  "daemon e2e: bash > executes a command and returns stdout",
  "daemon e2e: bash > a timeout-killed command resolves with exitCode=-1 (does not hang)",
  "daemon e2e: bash > background mode returns a taskId + status",
  "daemon e2e: fs > write + edit round-trip",
  "daemon e2e: fs > edit of a missing string returns 400",
  "daemon e2e: fs > unlink reports existed=true then existed=false",
  "daemon e2e: fs > mkdir + rename happy paths",
  "daemon e2e: fs > mkdir rejects a path escaping the root",
  "daemon e2e: fs > glob finds a written file (no ripgrep needed)",
  "daemon e2e: fs > grep finds matching content (strict in CI; tolerant locally without ripgrep)",
  "daemon e2e: config > GET /_sandbox/config on a fresh daemon returns null config + empty envKeys",
  "daemon e2e: config > POST /_sandbox/config bootstraps and reports the transition",
  "daemon e2e: config > POST /_sandbox/config with a non-object body returns 400",
  "daemon e2e: config > rejects changing the immutable cloneUrl with 409",
  "daemon e2e: config > env keys round-trip via GET (keys only, no values)",
  "daemon e2e: config > auth.rotateToken with a too-short token returns 400",
  "daemon e2e: tasks > GET /_sandbox/tasks lists a running background task",
  "daemon e2e: tasks > GET /_sandbox/tasks/:id returns a summary; unknown id is 404",
  "daemon e2e: tasks > kill a running task → ok; killing again → 400 not running",
  "daemon e2e: tasks > POST /_sandbox/tasks/kill-all returns a killed count",
  "daemon e2e: tasks > GET /_sandbox/tasks/:id/stream replays output then ends",
  "daemon e2e: tasks > DELETE a finished task → ok; deleting a running task → 400",
  "daemon e2e: orgfs-config > returns { written: false } when no sidecar path is configured",
  "daemon e2e: orgfs-config > invalid org-fs config returns 400",
  "daemon e2e: orgfs-config > writes the config to the sidecar control path when configured"
]
