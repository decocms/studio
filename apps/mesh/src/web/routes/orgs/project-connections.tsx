/**
 * Project-level Connections page.
 * Re-uses the org-admin connections UI so the storefront project
 * has its own /connections route without navigating away.
 */
export { default } from "./connections.tsx";
