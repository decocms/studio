{
  "name": "@decocms/benchmark",
  "version": "1.0.0",
  "private": true,
  "description": "Virtual MCP Benchmark Suite",
  "type": "module",
  "scripts": {
    "benchmark": "bun run benchmark.ts",
    "benchmark:quick": "bun run benchmark.ts --quick",
    "benchmark:high": "bun run benchmark.ts --high",
    "check": "echo 'Skipping type check - benchmark relies on mesh internals'"
  },
  "dependencies": {
    "decocms": "workspace:*",
    "@modelcontextprotocol/sdk": "1.26.0",
    "@openrouter/ai-sdk-provider": "^1.5.4",
    "ai": "^6.0.1",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "typescript": "^5.9.3"
  }
}
