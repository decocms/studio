---
title: Quickstart
description: From zero to a working agent in 5 minutes
icon: Rocket
---

import Callout from "../../../../components/ui/Callout.astro";

## Get Studio running

There are two ways to start.

### Option A — Managed cloud

Sign up at [studio.decocms.com](https://studio.decocms.com) and you're ready to go.

1. Create your account (email, GitHub, or Google)
2. Create an organization — your top-level workspace
3. You're in.

### Option B — Run locally

```bash
bunx decocms
```

This spins up a local instance with embedded PostgreSQL. Fully private — everything stays on your machine. Or clone and run from source:

```bash
git clone https://github.com/decocms/studio.git
cd studio
bun install
bun run dev
```

---

## AI provider (already set up for you)

New organizations start with credits on the **Deco AI Gateway**, so you can begin chatting with agents right away — no setup required.

When you want to use your own keys (Anthropic, Google, OpenRouter, etc.) or check your remaining gateway credit, head to **Settings → AI Providers**. See [AI Providers](/en/studio/ai-providers) for the full list.

---

## Add a connection

Connections are the tools your agents can use — GitHub, Slack, Shopify, your database, your own MCP server.

1. Open **Settings → Connections**
2. Search the catalog (e.g., Gmail), or click **Custom Connection** to add an unlisted MCP server
3. Click **Connect** and follow the sign-in or paste-a-key flow

Credentials are encrypted and never exposed to your team.

The catalog is populated by the registries enabled under **Settings → Store**.

---

## Create or open an agent

The sidebar shows your agents. Click an existing one or create a new one from the **+** button.

When you're inside an agent, the screen has three parts:

- **Left** — the list of threads (past conversations with this agent)
- **Center / bottom** — the chat input
- **Right** — the agent's panel: **Settings** (instructions, connections), **Automations**, and **Git** (if linked)

Open the **Settings** tab on the right, attach the connection you just added, and write a one-line instruction describing what this agent should do.

---

## Talk to it

Just type. The agent uses the tools you attached.

```
Send an email to me@example.com with a poem about MCP.
```

Watch the chat run. Each tool call is logged in **Settings → Monitor**.

<Callout type="success">
  You're set up! From here, set up [Automations](/en/studio/automations) to run agents on a schedule or event, invite team [members](/en/studio/user-management), or build more agents.
</Callout>

## Next steps

- **[Key concepts](./concepts)** — Anatomy of Studio: the agent shell vs. Settings
- **[Agents](./agents)** — How to design and configure agents
- **[Decopilot](./decopilot/overview)** — The AI assistant built into Studio
- **[Self-hosting](./self-hosting/quickstart)** — Deploy on Docker Compose or Kubernetes
