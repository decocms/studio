---
title: Automations
description: Agents that run on a schedule or in response to events, without a human in the loop
icon: Zap
---

import Callout from "../../../../components/ui/Callout.astro";

## What are automations?

Automations are agent runs that happen on their own — on a schedule, or when something occurs in a connected service. No one needs to be watching. The agent runs, does its job, and the result shows up in [Monitoring](/en/studio/monitoring).

Each automation has:

- **An agent** — the one doing the work
- **A prompt** — what it should do each time it runs
- **A model** — which AI and credentials to use
- **A trigger** — when it fires (on a schedule, on an event, or manually)

<Callout type="warning">
  Automations approve all tool calls automatically — there's no human review step. Scope your agent's tools carefully so it can only do what you intend.
</Callout>

## When to use automations

- **Scheduled tasks** — daily reports, data syncs, cleanup jobs
- **Event-driven workflows** — new order → start fulfillment, new support ticket → triage, deployment → send notification
- **Anything that should run without someone triggering it manually**

If you want a human in the loop — reviewing actions before they happen — use [Decopilot](/en/studio/decopilot/overview) instead. Automations are for work you've already validated and want to run hands-free.

## Creating an automation

1. Go to **Automations** in the sidebar
2. Click **New Automation**
3. Choose an agent
4. Write the prompt — what should it do each run?
5. Select a model and credentials
6. Add a trigger (schedule, event, or both)
7. Activate it

## Triggers

### Schedule

Runs the automation on a recurring schedule. Times are in UTC.

| Expression | When it runs |
|---|---|
| `0 9 * * *` | Every day at 9:00 AM |
| `0 */6 * * *` | Every 6 hours |
| `0 9 * * 1` | Every Monday at 9:00 AM |
| `*/5 * * * *` | Every 5 minutes |

Minimum interval is 60 seconds. If the system restarts, any missed runs are caught and fired on the next cycle.

### Event

Fires when a connected service publishes a matching event. You specify the connection, the event type, and optional filters — only events that match all filters will trigger the automation.

## Limits

- Maximum 3 runs per automation at the same time
- Maximum 10 automation runs across your organization at once
- Each run times out after 5 minutes
- Event chains are limited to 3 levels deep (to prevent loops)
- Event payloads are capped at 1 MB

If the person who created an automation leaves the organization, it's automatically deactivated.

---

**Next:** See [Agents](/en/studio/agents) to design what your automation will run, or [Monitoring](/en/studio/monitoring) to track results.
