---
title: Agentic CMS
description: A visual CMS for storefronts and content sites, with AI agents that operate the site with you
icon: LayoutTemplate
---

import Callout from "../../../../components/ui/Callout.astro";

## What is the Agentic CMS?

The **Agentic CMS** is the next generation of [deco.cx](https://docs.deco.cx) — a visual CMS for storefronts and content-heavy digital experiences, built on top of [deco Studio](/en/studio/overview). It's the flagship of deco's [Digital Experience suite](/en/studio/digital-experience).

It's two things in one:

- **A CMS** — type-safe React components, TypeScript loaders, theme editor, Git-based versioning, real-time collaboration between developers and content teams. The same model deco.cx established, refined.
- **A team of agents** — focused AI workers that author content, adjust the design, enrich the catalog, and keep the site healthy. They work alongside your team and you stay in control.

## Who it's for

Teams managing **storefronts** (ecommerce, marketplaces) and **content-driven sites** (media, branded D2C, knowledge bases) who want:

- The collaboration and performance of a code-first stack
- The speed and reach of a visual editor
- AI agents that scale the operational work — without hiding what they're doing

It replaces the boundary between "the engineering team that maintains the site" and "the content team that fills it." Both work in the same product, with agents handling the toil in between.

## Git-native, by design

Every change to your site — whether a person made it from the visual editor or an agent made it from chat — flows through the same Git workflow you already know:

1. The change is committed to a branch
2. A pull request is opened with a diff your team can review
3. Reviewers approve or request changes
4. On merge, the change ships to your site

This is the same model deco.cx pioneered, and it carries over directly. **Agents don't bypass review.** They open PRs like anyone else, and your team stays the source of truth — your repo, your branches, your history. Roll back, audit, and trace any change to its author, human or agent.

## How it relates to deco.cx

If you're coming from deco.cx, the Agentic CMS is the same product line — same core concepts (sections, loaders, blocks, theme editor, Decopilot), now plugged into Studio so your storefront ships with a full agent control plane around it.

| deco.cx concept | Agentic CMS today |
|---|---|
| Sections (React/TS components) | Same, with optional agent-authored variants |
| Loaders (data fetching) | Same, now visible as tools to your agents |
| Blocks (reusable units) | Same |
| Theme Editor | Same |
| Decopilot | Promoted to a first-class assistant scoped to your site |
| Admin UI | Now part of the Studio agent shell |
| Git workflow | Same — every change goes through PR review |

Existing deco.cx projects continue to work. Migration adds the agent layer; it doesn't replace your codebase.

## Sections editor

The **Content** tab in the agent workspace is the visual editor for the sections and pages that make up your storefront. It lists two groups — **Saved sections** (reusable global blocks, grouped by their underlying component) and **Available sections** (every component in the manifest catalog) — searchable from a single input.

- Selecting an **available** section opens a form editor for its props. Use **Save as global section** to persist your configuration as a reusable, named block.
- Selecting a **saved** section opens the same form editor, plus a toggle to switch to a **JSON view** (Monaco-based) for direct editing. Both views stay in sync — edit either one and the other reflects it immediately.

### Variants

Each section or page can have multiple **variants** — alternate configurations with different props or matcher-controlled conditions (A/B tests, audience segments, date windows). Variants appear as a list under their parent section in the editor.

You can:

- **Reorder variants** by dragging them up or down in the list. The order controls evaluation priority when multiple matchers could apply.
- **Preview a specific variant** by clicking it — the preview pane immediately applies that variant so you see exactly what a visitor matching that condition would see.
- **Duplicate a variant** from its menu — clones the variant's rule and sections right after it, and selects the clone so you can tweak it immediately.
- **Save as a global section** to persist a configured variant as a reusable block available across your entire storefront.

<Callout type="tip">
  Scroll position in the preview is preserved when you switch between variants or save changes, so you can stay focused on the section you're editing without losing your place on the page.
</Callout>

### Matcher field types

Matcher rules (the conditions that control which variant applies) support rich field types beyond plain text:

- **Inline unions** — a field that accepts one of several shapes (for example, "target by location" *or* "target by map area") renders as a branch selector. Pick a branch and only its relevant fields show; switching branches resets to that branch's defaults.
- **Location** — a cascading country → region → city picker for location-based matchers, including an interactive state map for Brazil.
- **Map area** — an interactive Google Map with a draggable, resizable circle for picking a center point and radius, used by location matchers that target a radius around a place.

### Variant calendar

The Content tab's **Calendar** view plots your scheduled variants across a month or timeline so you can see what's live, upcoming, or expired at a glance.

- Variants can be **open-ended** — with only a start or only an end date. Open-ended variants are hidden by default (they'd otherwise fill every day) behind a **"Show ongoing (N)"** toggle in the header.
- Bars are time-of-day accurate — a campaign ending at 10 AM doesn't visually fill its entire last day.

## Getting started

Open Studio, find the **deco Site Editor** agent and point it at your site.

<Callout type="info">
  The Agentic CMS isn't a separate product — it's a configuration of Studio + the deco.cx CMS, packaged for storefront teams. Everything you learn about Studio agents, connections, and automations applies directly.
</Callout>
