# ============================================
# Docker Compose - Environment Variables
# ============================================
# Copy this file to .env and adjust values as needed
# cp conf-examples/env.example .env

# ============================================
# Docker Image
# ============================================
IMAGE_REPOSITORY=ghcr.io/decocms/mesh/mesh
IMAGE_TAG=latest

# ============================================
# Application Port
# ============================================
# Port that will be exposed on host (default: 3000)
PORT=3000

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
# NODE_ENV=development

# ============================================
# Application URLs
# ============================================
# URLs used for authentication and application links
# Adjust according to your environment (e.g.: https://mesh.example.com)
BETTER_AUTH_URL=http://localhost:3000
BASE_URL=http://localhost:3000

# ============================================
# Secrets (REQUIRED)
# ============================================
# Generate a secure secret with: openssl rand -base64 32
# IMPORTANT: Change this value in production!
BETTER_AUTH_SECRET=change-this-to-a-secure-random-string-at-least-32-chars

# ============================================
# Database
# ============================================
# Embedded PostgreSQL (default) - no additional configuration required
# DATABASE_URL will be automatically mounted by docker-compose.yml if you define POSTGRES_USER below
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# PostgreSQL (optional) - configure the variables below to automatically mount DATABASE_URL
# docker-compose.yml will automatically mount DATABASE_URL using these variables
# If you define POSTGRES_USER, DATABASE_URL will be automatically mounted
# POSTGRES_USER=mesh_user
# POSTGRES_PASSWORD=secure_password_here
# POSTGRES_DB=mesh_db
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# Alternatively, you can define DATABASE_URL manually to override everything above:
# DATABASE_URL=postgresql://user:password@host:port/database_name
