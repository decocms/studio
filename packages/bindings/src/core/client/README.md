# Client Implementation

This folder is basically a duplicated implementation based on runtime/binder.ts client. We should duplicate for now since we are remodeling the way applications invoke Studio.
