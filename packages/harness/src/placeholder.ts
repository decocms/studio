// Scaffold placeholder so `src/` exists and `tsc --noEmit` has an input
// (TS18003 otherwise). The harness tree is `git mv`'d in by the next task,
// at which point this file is deleted.
export {};
