---
phase: 20-public-report-ui
plan: "03"
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - apps/mesh/src/web/components/report/pro-badge.tsx
  - apps/mesh/src/web/components/report/traffic-section.tsx
  - apps/mesh/src/web/components/report/seo-rankings-section.tsx
  - apps/mesh/src/web/components/report/brand-section.tsx
  - apps/mesh/src/web/components/report/percentile-section.tsx
  - apps/mesh/src/web/routes/report.tsx
autonomous: true
requirements:
  - DIAG-07
  - DIAG-08
  - DIAG-09
  - DIAG-10
  - RPT-03

must_haves:
  truths:
    - "Traffic volume and competitor comparison section is visible on the report with mocked data"
    - "SEO rankings and backlinks section is visible on the report with mocked data"
    - "Brand/visual identity section is visible on the report with mocked data"
    - "Percentile comparison section is visible on the report with mocked data"
    - "Each mocked section has a visible Pro or upgrade indicator badge"
  artifacts:
    - path: "apps/mesh/src/web/components/report/pro-badge.tsx"
      provides: "Reusable Pro/upgrade indicator badge component"
      min_lines: 10
    - path: "apps/mesh/src/web/components/report/traffic-section.tsx"
      provides: "Mocked traffic volume and competitor comparison"
      min_lines: 30
    - path: "apps/mesh/src/web/components/report/seo-rankings-section.tsx"
      provides: "Mocked SEO rankings and backlinks"
      min_lines: 30
    - path: "apps/mesh/src/web/components/report/brand-section.tsx"
      provides: "Mocked brand/visual identity extraction"
      min_lines: 25
    - path: "apps/mesh/src/web/components/report/percentile-section.tsx"
      provides: "Mocked percentile comparison vs other storefronts"
      min_lines: 25
  key_links:
    - from: "apps/mesh/src/web/routes/report.tsx"
      to: "apps/mesh/src/web/components/report/traffic-section.tsx"
      via: "component import"
      pattern: "import.*TrafficSection"
    - from: "apps/mesh/src/web/components/report/traffic-section.tsx"
      to: "apps/mesh/src/web/components/report/pro-badge.tsx"
      via: "ProBadge import"
      pattern: "import.*ProBadge"
---

<objective>
Add mocked "Pro" sections to the report page — traffic/competitors, SEO rankings/backlinks, brand/visual identity, and percentile comparison — each with a visible "Pro" upgrade indicator.

Purpose: These sections show realistic but mocked data that becomes real in a future version. They demonstrate the platform's full potential and create upgrade motivation. Each must be visually marked as "Pro" content.

Output: Four mocked section components integrated into the report page, each displaying realistic mocked data with a "Pro" badge. The ProBadge component is reusable across all mocked sections.
</objective>

<execution_context>
@/Users/rafaelvalls/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rafaelvalls/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-public-report-ui/20-02-SUMMARY.md

Key files to read before implementing:
@apps/mesh/src/web/routes/report.tsx — Report page created in Plan 02, contains placeholder for mocked sections
@apps/mesh/src/web/components/report/performance-section.tsx — Example of a real section component (follow same patterns)
@packages/ui/src/components/badge.tsx — Badge component from design system (may be useful for ProBadge)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pro badge and mocked section components</name>
  <files>
    apps/mesh/src/web/components/report/pro-badge.tsx
    apps/mesh/src/web/components/report/traffic-section.tsx
    apps/mesh/src/web/components/report/seo-rankings-section.tsx
    apps/mesh/src/web/components/report/brand-section.tsx
    apps/mesh/src/web/components/report/percentile-section.tsx
  </files>
  <action>
    **Important constraints:**
    - React 19 — NO useEffect, NO useMemo/useCallback/memo
    - Tailwind v4 design system tokens
    - All mocked data is hardcoded in the components (static constants)
    - Each mocked section must include the ProBadge
    - The mocked data should look realistic but not be confused with real data

    **Create `apps/mesh/src/web/components/report/pro-badge.tsx`:**
    - Small, eye-catching badge that says "Pro" or "Upgrade to Pro"
    - Styled with a gradient or bold color to stand out (e.g., purple/violet gradient, or gold accent)
    - Can be inline (next to section heading) or as a floating overlay
    - Props: `{ label?: string }` with default "Pro"
    - Example: a small pill badge with a sparkle/lock icon and "Pro" text
    - Should contrast with the report's neutral document style to draw attention

    **Create `apps/mesh/src/web/components/report/traffic-section.tsx`:**
    - DIAG-07: Traffic volume and competitor comparison
    - ProBadge in the section header
    - **Mocked data (hardcoded constants at top of file):**
      - Monthly visits: "~145K" with a trend arrow ("+12% vs last month")
      - Traffic sources breakdown: Organic 42%, Direct 28%, Paid 18%, Social 8%, Referral 4%
      - Top competitors table (3-4 rows): competitor domain, estimated monthly visits, overlap %
        - Example: "competitor-a.com — ~210K visits — 34% audience overlap"
    - **Layout:** Traffic overview card at top, sources as a horizontal bar or simple breakdown, competitors as a compact table/list
    - Apply a subtle blur/opacity overlay or glassmorphism effect on the data to hint it's locked behind Pro, OR show the data clearly but with the ProBadge prominently displayed. Claude's discretion — the key requirement is that the Pro indicator is clearly visible (RPT-03).

    **Create `apps/mesh/src/web/components/report/seo-rankings-section.tsx`:**
    - DIAG-08: SEO ranking and backlink data
    - ProBadge in the section header
    - **Mocked data:**
      - Top ranking keywords (5-6): keyword, position, search volume, URL
        - Example: "wireless headphones — #8 — 12,100/mo — /products/headphones"
      - Backlink summary: total backlinks count (e.g., "2,340"), referring domains (e.g., "187"), domain authority (e.g., "42/100")
    - **Layout:** Keywords as a compact table, backlink stats as 3 metric cards

    **Create `apps/mesh/src/web/components/report/brand-section.tsx`:**
    - DIAG-09: Brand/visual identity extraction
    - ProBadge in the section header
    - **Mocked data:**
      - Primary colors: 3-4 color swatches (hex codes with colored circles)
        - Example: #1a1a2e, #e94560, #0f3460, #16213e
      - Typography: font family names detected (e.g., "Inter", "Georgia")
      - Logo presence: "Logo detected" checkmark
      - Brand consistency score: "78/100" (mocked)
    - **Layout:** Color swatches as colored circles/squares, fonts as text samples, score as a gauge or simple number

    **Create `apps/mesh/src/web/components/report/percentile-section.tsx`:**
    - DIAG-10: Percentile comparison vs other storefronts
    - ProBadge in the section header
    - **Mocked data:**
      - Overall score: "67th percentile" (out of all analyzed storefronts)
      - Breakdown by category: Performance (72nd), SEO (58th), Tech Stack (81st), Content (45th)
      - Visual: horizontal bars or a radar/spider-like indicator (simple CSS bars, not a chart library)
    - **Layout:** Overall percentile prominently displayed, category breakdown as labeled progress bars

    **Styling for all mocked sections:**
    - Match the visual style of the real sections from Plan 02 (same heading size, spacing, card style)
    - The ProBadge should be consistent across all 4 sections
    - Consider a subtle visual distinction (e.g., slightly different background tint or a thin colored left border) to differentiate mocked from real sections — but keep it subtle, not jarring
  </action>
  <verify>
    Run `bun run check` — all new files should pass TypeScript checks.
    Run `bun run lint` — no useEffect/useMemo violations.
    Run `bun run fmt` to ensure formatting.
  </verify>
  <done>
    Five new components created: ProBadge, TrafficSection, SeoRankingsSection, BrandSection, PercentileSection. Each mocked section has hardcoded realistic data and includes the ProBadge component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mocked sections into the report page</name>
  <files>
    apps/mesh/src/web/routes/report.tsx
  </files>
  <action>
    **Modify `apps/mesh/src/web/routes/report.tsx`:**

    1. Import the 4 mocked section components:
       ```typescript
       import { TrafficSection } from "@/web/components/report/traffic-section";
       import { SeoRankingsSection } from "@/web/components/report/seo-rankings-section";
       import { BrandSection } from "@/web/components/report/brand-section";
       import { PercentileSection } from "@/web/components/report/percentile-section";
       ```

    2. Replace the placeholder comment (`{/* Mocked Pro sections — added by Plan 03 */}`) with the actual components:
       ```tsx
       <TrafficSection />
       <SeoRankingsSection />
       <BrandSection />
       <PercentileSection />
       ```

    3. The mocked sections take no props — they render their own hardcoded data.

    4. Ensure the section order matches RPT-01: performance, SEO, tech stack, company context, then traffic, competitors (in TrafficSection), SEO rankings, brand, percentile.

    The sections should have the same spacing and visual rhythm as the real sections above them.
  </action>
  <verify>
    Run `bun run check` — report.tsx should pass with the new imports.
    Run `bun run lint` — no issues.
    Run `bun run fmt` to ensure formatting.
    Verify all 4 mocked sections appear after the real sections in the report layout.
  </verify>
  <done>
    Report page at `/report/<token>` now shows all 8 sections: 4 real data sections followed by 4 mocked Pro sections. Each mocked section has a visible Pro/upgrade indicator.
  </done>
</task>

</tasks>

<verification>
1. `bun run check` passes — no TypeScript errors
2. `bun run lint` passes — no banned hook usage
3. `bun run fmt` passes — code formatted
4. All 4 mocked sections visible on the report page
5. Each mocked section includes a ProBadge/upgrade indicator
6. Mocked data looks realistic (traffic numbers, keywords, colors, percentiles)
7. Report section order: performance, SEO, tech stack, company context, traffic, SEO rankings, brand, percentile
</verification>

<success_criteria>
- Traffic section shows mocked visitor count, sources, and competitor comparison with Pro badge
- SEO rankings section shows mocked keyword rankings and backlink data with Pro badge
- Brand section shows mocked color palette, typography, and brand score with Pro badge
- Percentile section shows mocked category percentiles with Pro badge
- All mocked sections are integrated into the report page after the real sections
- Pro badge is visually distinct and clearly indicates upgrade content
- All quality checks pass (TypeScript, lint, format)
</success_criteria>

<output>
After completion, create `.planning/phases/20-public-report-ui/20-03-SUMMARY.md`
</output>
